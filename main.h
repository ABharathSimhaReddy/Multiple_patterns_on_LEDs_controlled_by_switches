#ifndef MAIN_H          // Prevent this header file from being included multiple times
#define MAIN_H          // Define the header guard

#define ON  1           // Macro to represent ON state (value = 1)
#define OFF 0           // Macro to represent OFF state (value = 0)

#endif                  // End of header guard
